# Data

Place the Home Credit CSV files in `data/raw/` before training:

- `application_train.csv`
- `bureau.csv`
- `previous_application.csv`
- `HomeCredit_columns_description.csv`

The CSVs are ignored by git because the raw dataset files are large.
